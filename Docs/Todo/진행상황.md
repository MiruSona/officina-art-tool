# ArtTool 진행 상황

> 이 툴 작업을 새로 이어받는 세션은 이 문서부터 읽는다. 마지막 고침 : 2026-09-06

## 한 줄 상태

스프라이트·타일·UI 세 갈래 설계 2장과 Python 구현(`src/arttool/` 4,337줄, 시험 374개 통과)이
2026-08-25 밤 ~ 08-26 아침에 끝났고, 코드 리뷰 2회 54건도 다 고쳤다.
**커밋은 2026-08-29 에 끝났다** (스튜디오 저장소 커밋 `da924ac`).

## 지금 차례 (순서대로)

1. **(끝 · 09-06)** UI 설계 문서 4-1 "규칙 다섯"을 **여섯**으로 한 줄 고친다. 코드 리뷰에서 PPU 대조가 규칙으로
   더 들어갔다 (`README.md` 의 `ui check` 표는 이미 "여섯"으로 맞음).
2. **Unity 실측.** C# 세 장(`src/arttool/ui/unity/`) 컴파일, TMP 폰트 굽기,
   `OnPreprocessTexture` 에서 `ISpriteEditorDataProvider` 가 되는지 본다. Unity 가 없어 여태 못 봤다.
3. 바깥 도구 연결(Aseprite·DeBroglie·PixelLab, 지금은 계약만 시험), 갈무리 ttf·Kenney 에셋 받기,
   `ui_panel` 램프 색 실제 값 정하기.
4. 2차로 미룬 나머지 목록은 `그림툴.md` 3-3 을 본다.

## 읽는 순서

| # | 무엇 | 왜 |
| --- | --- | --- |
| 1 | `README.md` | 명령 목록 · 4단 파이프라인 · 폴더 지도 · 종료 코드 |
| 2 | `Docs/Design/2026-08-25-그림툴설계.md`, `2026-08-25-UI툴설계.md` | 스프라이트·타일·UI 셋의 근거 |
| 3 | `Docs/Todo/그림툴.md` (특히 3-3) | 정해진 것과 2차로 미룬 것 |
| 4 | `Docs/Guide/AI-그래픽-캐릭터-배경-가이드.html` | 캐릭터·배경 그림을 AI 로 만들 때의 안내 (브라우저로 연다) |
| 5 | `mem search arttool` | 결정·주의·공수 |

## 주의

- **east 그림은 규격을 맞춘(fit_frame) west 결과를 뒤집어 만든다.** 원본을 먼저 뒤집으면 가운데 맞추기가
  다시 걸려 앵커 공식과 1~2px 어긋난다 (`mem show 20260825-f6df9e26`).
- **TMP 폰트는 `Dynamic` 으로 만들어 글자를 다 넣은 뒤 `Static` 으로 잠근다.** 처음부터 `Static` 이면
  `TryAddCharacters` 가 조용히 실패해 빈 폰트가 구워진다 (`mem show 20260825-2674917e`).
- **에디터 전용 C# 은 `Editor` 폴더 안에 둔다.** 밖에 두면 `UnityEditor` 네임스페이스 때문에
  플레이어 빌드가 컴파일 오류로 깨진다 (`mem show 20260825-25877857`).
- 시험 돌리는 법 (`README.md` 「설치」 참고) — 스튜디오에서는 `ArtTool/.venv/Scripts/python -m pytest ArtTool -q`,
  ArtTool 저장소 단독에서는 `.venv/Scripts/python -m pytest -q`

결정·교훈·공수·할 일은 mem 에 있다. `mem search arttool` 로 찾는다.
